name: CI - SonarCloud + Build/Publish

on:
  push:
    branches:
      - '**'   # qualquer push
  pull_request:
    branches:
      - '**'   # qualquer PR

jobs:
  sonarcloud:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Build classes if main
        if: github.ref == 'refs/heads/main'
        run: mvn clean compile -Pproduction

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        with:
          args: >
            -Dsonar.projectKey=JuniorOperationsTeam_ToDoApp
            -Dsonar.organization=junioroperationsteam
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.token=${{ secrets.SONAR_TOKEN }}
            -Dsonar.branch.name=${{ github.head_ref || github.ref_name }}
            ${{ github.ref == 'refs/heads/main' && '-Dsonar.java.binaries=target/classes' || '' }}

  build-and-upload:
    runs-on: ubuntu-latest
    needs: sonarcloud
    if: github.ref == 'refs/heads/main'   # só corre no main
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Build JAR
        run: mvn clean package -Pproduction

      - name: Copy JAR to project root
        run: cp target/*.jar .

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: ToDoApp-jar
          path: '*.jar'
